# 새 컴퓨터에서 MyPace 작업 시작하기

> 데스크탑/노트북 번갈아 쓸 때, 새 컴퓨터에서 **딱 한 번** 이 파일대로 하면 됩니다.
> Firebase Studio가 만료돼도 GitHub만 있으면 어디서든 이어서 작업 가능.

## 1단계 — Claude Code 켜고 이 한 마디만

새 컴퓨터에서 Claude Code를 설치하고 켠 뒤, 이렇게 말하세요:

> **"이 폴더의 `새컴퓨터-세팅.md` 파일 보고 그대로 세팅해줘. `.env.local`도 만들어줘."**

(이미 클론을 안 했다면 먼저 이렇게: **"`https://github.com/nickkim931215/mypace.git` 클론하고 세팅해줘"**)

그러면 Claude가 알아서 `npm install` 하고, 아래 `.env.local`을 만들고, 실행까지 확인해줍니다. **직접 복사할 필요 없어요.**

## 2단계 — `.env.local` 내용 (Claude가 이걸로 파일 만들어줌)

프로젝트 폴더 안에 `.env.local` 파일을 만들고 아래 내용을 넣습니다.
(아래 `NEXT_PUBLIC_` 값들은 원래 앱에 공개되는 값이라 안전합니다.)

```
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyDe-5qyrTl22-rkv0jeTUCAylk7NuP66xk
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=mypace-5379e.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=mypace-5379e
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=mypace-5379e.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=60962735495
NEXT_PUBLIC_FIREBASE_APP_ID=1:60962735495:web:fae6d84feb68cef0a37f22
GOOGLE_GENERATIVE_AI_API_KEY=
ADMIN_EMAIL=nickkim931215@gmail.com
```

## 3단계 — 실행

```
npm run dev
```

브라우저에서 잘 뜨면 끝.

---

## 평소 습관 (★중요)

두 컴퓨터를 안 꼬이게 쓰는 규칙:

- **작업 시작할 때** → Claude한테 "최신본 받아줘" (`git pull`)
- **작업 끝낼 때** → Claude한테 "GitHub에 올려줘" (`git push`)

> 한 컴퓨터에서 끝내면 꼭 **올려줘**, 다른 컴퓨터에서 시작할 땐 꼭 **받아줘**.
> 그래야 작업이 항상 따라다니고 안 사라집니다.
